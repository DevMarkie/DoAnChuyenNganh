# Optional: Backend API URL.
# If empty, frontend defaults to '/api' (which uses Vite proxy in dev or Nginx in production).
# In separate cloud deployment (e.g. Vercel frontend + Render backend), set this to your backend domain:
# VITE_API_URL=https://your-backend-service.onrender.com/api
VITE_API_URL=
